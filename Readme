1. Create a database name "loginsystem"

2. Create TABLE users (user_id Integer(100) AUTO INCREMENT NOT NULL PRIMARY KEY,
user_first VARCHAR(250), user_last VARCHAR(250), user_email VARCHAR(250), user_uid VARCHAR(250), user_pwd(250)
);
